const path = require("path");

module.exports = {
  config: path.resolve("src", "config", "config.json"), // Path to config.json
  "models-path": path.resolve("src", "models"),         // Path to models folder
  "migrations-path": path.resolve("src", "migrations"), // Path to migrations folder
  "seeders-path": path.resolve("src", "seeders"),       // Path to seeders folder
};